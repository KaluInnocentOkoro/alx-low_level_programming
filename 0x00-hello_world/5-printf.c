#include <stdio.h>

/**
* main - Entry Point
*
* Return: 0 always
*/
int main(void)
{
	char mystr[] = "with proper grammar, but the outcome is a piece of art,";

	printf("%s\n", mystr);
	return (0);
}
